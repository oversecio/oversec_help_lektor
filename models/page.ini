[model]
name = Page
label = {{ this.title }}

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown

[fields.sortstring]
label = Sortstring
type = string

[fields.redirect]
label = Redirect based on browser locale, set this ONLY on the default main index page to avoid redirect loops! 
type = boolean

